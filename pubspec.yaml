name: dz_freelance_platform
description: منصة فريلانس محلية للجزائر - Flutter + Riverpod + GoRouter (MVP)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.2.7
  flutter_riverpod: ^2.5.1
  intl: ^0.19.0
  uuid: ^4.5.1
  shared_preferences: ^2.3.2
  file_picker: ^8.1.4
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
